
export default async function ProductsPage() {


  return (
    <div style={{ padding: 24 }}>
      {/* <h1>Products ({products.length})</h1>
      <ul>
        {products.map((p) => (
          <li key={p.id}>
            {p.name} — {p.brand?.name} — {p.overallScore}/10
            {p.isRecommended && ' ⭐'}
          </li>
        ))}
      </ul>

      <h1 style={{ marginTop: 24 }}>Categories ({categories.length})</h1>
      <ul>
        {categories.map((c) => (
          <li key={c.id}>{c.nameTh || c.nameEn} ({c.slug})</li>
        ))}
      </ul> */}
    </div>
  );
}
